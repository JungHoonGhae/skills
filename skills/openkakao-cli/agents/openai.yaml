interface:
  display_name: "OpenKakao CLI"
  short_description: "Send/read KakaoTalk on macOS without login, via Accessibility automation"
  default_prompt: "Use openkakao-cli to send and read KakaoTalk messages on macOS. Prefer the login-free commands local-send and ax-read (Accessibility automation, no server session); server login is broken on recent builds."
